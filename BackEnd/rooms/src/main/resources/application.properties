# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/rooms
# Define the database URL. In this case, it's a MySQL database on the localhost and port 3306.
spring.datasource.username=root
# Database username for authentication.
spring.datasource.password=Toby67304
# Database password for authentication.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Define the JDBC driver class for MySQL.

# JPA (Java Persistence API) Configuration
spring.jpa.show-sql=true
# Enable logging of SQL queries to the console. Useful for debugging.
spring.jpa.hibernate.ddl-auto=update
# Automatically update the database schema based on the entity classes.